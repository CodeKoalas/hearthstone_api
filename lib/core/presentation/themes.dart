import 'package:flutter/material.dart';

/// Logical "blocks" of space.
class Spacing {
  static const double one = 5;
  static const double two = one * 2.0;
  static const double three = one * 3.0;
  static const double four = two * 2.0;
  static const double five = one * 5.0;
  static const double six = two * 3.0;
  static const double ten = two * 5.0;
  static const double bottomSpacing = ten + kBottomNavigationBarHeight;
  static const double topSpacing = kToolbarHeight;
}
